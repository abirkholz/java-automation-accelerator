package driver;

import org.openqa.selenium.remote.RemoteWebElement;

/**
 * Driver
 * This class is essentially a wrapper around the Selenium/Appium driver.
 * Add methods that combine driver functionality but are re-usable across all UI tests.
 */
public class Driver {

    /**
     * scrollToElement
     * Use this method for elements that require scrolling.
     * @param Element - the element that you want to scroll to.
     */
    public void scrollToElement(RemoteWebElement Element){
        //TODO
    }
}
